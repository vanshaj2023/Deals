import React from 'react';
import Wishlist from '@/components/Wishlist'; // Adjust the import path if your DealsPage component is in a different directory

const Deals: React.FC = () => {
    return <Wishlist />;
};

export default Deals;
